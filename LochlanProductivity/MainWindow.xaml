<?xml version="1.0" encoding="utf-8"?>

<Window
    x:Class="LochlanProductivity.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LochlanProductivity"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="LochlanProductivity">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Grid Background="#99F2F3F0" Padding="28">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER — minimal -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Spacing="4">
                <TextBlock Text="Today's Tasks" FontSize="30" FontWeight="SemiBold" FontFamily="Cambria" Foreground="#FF3A2E28"/>
                <TextBlock Text="Complete your tasks to unlock — focus stays frost-clean until done." FontSize="12.5" FontFamily="Segoe UI Variable Text" Foreground="#FF6B5E52" Opacity="0.95"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <Ellipse x:Name="SyncStatusDot" Width="8" Height="8" Fill="#FF8A9A8B" VerticalAlignment="Center"/>
                    <TextBlock x:Name="SyncStatusText" Text="Synced" FontSize="11" FontFamily="Segoe UI Variable Small" Foreground="#FF6B5E52" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Content="..." FontSize="16" Padding="10,4" Background="#CCFAFBF9" BorderBrush="#FFDDE3E0" Foreground="#FF3A2E28" CornerRadius="8">
                    <Button.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="App Groups" Click="ManageAppGroups_Click"/>
                            <MenuFlyoutItem Text="Scheduled Blocking" Click="ManageSchedules_Click"/>
                            <MenuFlyoutItem Text="Scheduled Tasks" Click="ScheduledTasks_Click"/>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem Text="Blocked Websites" Click="BlockedSitesButton_Click"/>
                            <MenuFlyoutItem Text="Sync Folder" Click="SyncFolderButton_Click"/>
                            <MenuFlyoutItem Text="Google Calendar" Click="CalendarButton_Click"/>
                            <MenuFlyoutSeparator/>
                            <MenuFlyoutItem Text="Start with Windows" Click="StartupToggle_Click"/>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
                <Button x:Name="StartupToggleButton" Visibility="Collapsed" Click="StartupToggle_Click"/>
            </StackPanel>
        </Grid>

        <!-- FOCUS CARD — frost glass (semi-transparent over Mica) -->
        <Border Grid.Row="1" Background="#CCFAFBF9" BorderBrush="#FFDDE3E0" BorderThickness="1" CornerRadius="14" Padding="16">
            <Border BorderBrush="#FF8E7D6B" BorderThickness="4,0,0,0" CornerRadius="8" Padding="12,2,0,2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Spacing="2">
                        <TextBlock x:Name="BlockingStatusText" Text="Focus Mode is OFF" FontSize="15" FontWeight="SemiBold" FontFamily="Cambria" Foreground="#FF3A2E28"/>
                        <TextBlock x:Name="BlockedAppStatusText" Text="No apps are currently being monitored." FontSize="12" FontFamily="Segoe UI Variable Text" Foreground="#FF6B5E52"/>
                        <TextBlock x:Name="CalendarStatusText" Visibility="Collapsed" FontSize="11" Foreground="#FF6B7C6E" TextWrapping="Wrap"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Spacing="8" VerticalAlignment="Center">
                        <Button x:Name="BlockingButton" Content="Start Focus Mode" Click="BlockingButton_Click" Padding="18,8" Background="#FF8A9A8B" Foreground="White" BorderThickness="0" CornerRadius="8"/>
                        <Button x:Name="EmergencyButton" Content="Emergency: Allow YouTube (15m)" Click="EmergencyButton_Click" Visibility="Collapsed" Background="#FFF8F0F0" Foreground="#FFA65D3C" BorderBrush="#FFE8CFCF" BorderThickness="1" CornerRadius="8" FontSize="11" Padding="10,4"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Border>

        <!-- NOTIFICATION -->
        <Border x:Name="BlockedAppNotification" Grid.Row="2" Margin="0,12,0,0" Padding="14" CornerRadius="12" Background="#FFF8F0F0" BorderBrush="#FFE8CFCF" BorderThickness="1" Visibility="Collapsed">
            <StackPanel>
                <TextBlock x:Name="BlockedAppNotificationTitle" FontSize="14" FontWeight="SemiBold" Foreground="#FF2E3440"/>
                <TextBlock x:Name="BlockedAppNotificationMessage" Margin="0,4,0,0" TextWrapping="Wrap" Foreground="#FF5A6460" FontSize="12"/>
            </StackPanel>
        </Border>

        <!-- TASK INPUT — frost glass -->
        <Border Grid.Row="3" Margin="0,14,0,14" Background="#CCFAFBF9" BorderBrush="#FFDDE3E0" BorderThickness="1" CornerRadius="12" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TaskInput" Grid.Column="0" PlaceholderText="What do you need to do?" Background="White" BorderBrush="#FFE8ECE8" CornerRadius="8" KeyDown="TaskInput_KeyDown"/>
                <Button Grid.Column="1" Content="Add" Margin="10,0,0,0" Click="AddTask_Click" Background="#FF8A9A8B" Foreground="White" BorderThickness="0" CornerRadius="8" Padding="16,6"/>
            </Grid>
        </Border>

        <!-- CONTENT — Today on left, Future + Long-term always visible on right -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.55*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- TODAY -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,8,0">
                <StackPanel x:Name="TaskList" Spacing="10"/>
            </ScrollViewer>

            <!-- RIGHT RAIL — Long-term on top, Future below, always visible -->
            <ScrollViewer Grid.Column="1" Margin="12,0,0,0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Spacing="18">
                    <StackPanel x:Name="LongTermSection" Spacing="8">
                        <TextBlock Text="Long-term &amp; Notes" FontSize="14" FontWeight="SemiBold" FontFamily="Cambria" Foreground="#FF3A2E28" Opacity="0.85"/>
                        <TextBlock Text="Ideas, goals, and info to remember — no due date, never blocks focus." FontSize="11" Foreground="#FF6B5E52" Opacity="0.7" TextWrapping="Wrap"/>
                        <Border Background="#CCFAFBF9" BorderBrush="#FFDDE3E0" BorderThickness="1" CornerRadius="12" Padding="10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="LongTermInput" Grid.Column="0" PlaceholderText="Add a long-term goal or note…" Background="White" BorderBrush="#FFE8ECE8" CornerRadius="8" KeyDown="LongTermInput_KeyDown"/>
                                <Button Grid.Column="1" Content="Add Note" Margin="10,0,0,0" Click="AddLongTerm_Click" Background="#FFE8ECE8" Foreground="#FF3A2E28" BorderThickness="0" CornerRadius="8" Padding="14,6"/>
                            </Grid>
                        </Border>
                        <StackPanel x:Name="LongTermTaskList" Spacing="10"/>
                    </StackPanel>

                    <StackPanel x:Name="FutureSection" Spacing="8">
                        <TextBlock Text="Coming Up" FontSize="14" FontWeight="SemiBold" FontFamily="Cambria" Foreground="#FF3A2E28" Opacity="0.85"/>
                        <TextBlock Text="Tasks scheduled for the future — they’ll appear unmarked on their day." FontSize="11" Foreground="#FF6B5E52" Opacity="0.7" TextWrapping="Wrap"/>
                        <StackPanel x:Name="FutureTaskList" Spacing="10"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>

    </Grid>

</Window>
